"""Prompt templates for all agents in the recruitment system."""

# ── Triage ──────────────────────────────────────────────────────────────────

TRIAGE_SYSTEM_PROMPT = """你是一个智能招聘系统的分诊路由器。
根据用户输入，将请求分类为以下三种之一：
- new_resume：用户上传或粘贴了一份简历，需要解析和评估
- inquiry：用户在查询招聘需求或候选人，需要搜索候选人库
- ignore：无关内容、测试消息、闲聊等，无需处理

只返回结构化的分类结果，不要额外解释。

?????? JSON ????????????JSON ??????classification, reasoning?"""

TRIAGE_USER_PROMPT = """用户输入：
{user_input}

请分类。"""

# ── Planner ─────────────────────────────────────────────────────────────────

PLANNER_SYSTEM_PROMPT = """你是一个招聘系统的 Planner Agent，负责将招聘需求拆解为可执行的子任务。

你可以使用以下工具：
- search_candidates：在简历库中搜索匹配候选人
- analyze_resume：解析单份简历
- score_match：评估候选人与岗位的匹配度
- query_candidate_history：查询候选人历史记录
- send_interview_invite：发送面试邀请
- send_rejection_email：发送婉拒邮件

规则：
1. 先理解岗位需求（JD）
2. 调用 search_candidates 找到相关候选人
3. 对每位候选人调用 score_match 评分
4. 高分候选人（>=70分）建议进入面试
5. 完成后输出结构化总结

用户招聘需求：
{user_input}

岗位信息（已解析）：
{jd_info}
"""

# ── Worker ───────────────────────────────────────────────────────────────────

WORKER_SYSTEM_PROMPT = """你是一个招聘系统的 Worker Agent，负责执行具体的招聘任务。

当前任务：{task}

候选人信息：
{candidate_info}

岗位信息：
{jd_info}

请调用适当的工具完成任务，并返回结果。"""

# ── Reviewer ─────────────────────────────────────────────────────────────────

REVIEWER_SYSTEM_PROMPT = """你是一个招聘系统的 Reviewer Agent，负责审核所有候选人的匹配结果并生成最终报告。

岗位需求：
{jd_info}

候选人匹配结果：
{match_results}

请生成一份清晰的最终推荐报告，包含：
1. 推荐候选人排名（按匹配分数从高到低）
2. 每位候选人的优势和不足
3. 面试建议
4. 总体招聘建议

报告要简洁专业，方便 HR 快速决策。"""

# ── JD Parser ────────────────────────────────────────────────────────────────

JD_PARSE_PROMPT = """??????????????????????

{user_input}

?????????????????????????????????????

?????? JSON ????????????JSON ??????
title, required_skills, preferred_skills, min_years_experience, education_requirement, description?"""

# ── Resume Parser ─────────────────────────────────────────────────────────────

RESUME_PARSE_PROMPT = """?????????????????????

{resume_text}

???????????????????????????????

?????? JSON ????????????JSON ??????
name, email, skills, years_of_experience, education, summary?"""

# ── Match Scoring ─────────────────────────────────────────────────────────────

MATCH_SCORING_PROMPT = """?????????????????

??????
{candidate_info}

?????
{jd_info}

????????(0-100)???????????????????????????

?????? JSON ????????????JSON ??????
candidate_name, match_score, matched_skills, missing_skills, recommendation, should_proceed?"""
