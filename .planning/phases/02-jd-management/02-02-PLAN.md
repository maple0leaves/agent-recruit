---
phase: 02-jd-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/types/jd.ts
  - frontend/src/api/jd.ts
  - frontend/src/hooks/useJDs.ts
  - frontend/src/components/ui/ (new shadcn components: table, form, select, badge, textarea, popover, calendar, dialog, command, dropdown-menu)
autonomous: true
requirements:
  - JD-01
  - JD-02
  - JD-03
  - JD-04

must_haves:
  truths:
    - "TypeScript types for JD, JDListResponse, JDFormValues, JDTemplate are defined and exportable"
    - "API functions (fetchJDs, fetchJDTemplates, createJD, updateJD, updateJDStatus) are available and call axios"
    - "TanStack Query hooks (useJDs, useJDTemplates, useCreateJD, useUpdateJD, useUpdateJDStatus) are defined"
    - "shadcn/ui components table, form, select, badge, textarea, popover, calendar, dialog, command, dropdown-menu are installed"
    - "New npm packages (@tanstack/react-table, react-hook-form, react-day-picker, date-fns) are in package.json"
  artifacts:
    - path: "frontend/src/types/jd.ts"
      provides: "All TypeScript interfaces for JD domain"
      contains: "export interface JD"
    - path: "frontend/src/api/jd.ts"
      provides: "Axios API functions for JD CRUD"
      contains: "fetchJDs"
    - path: "frontend/src/hooks/useJDs.ts"
      provides: "TanStack Query hooks with pagination support"
      contains: "useJDs"
  key_links:
    - from: "frontend/src/api/jd.ts"
      to: "frontend/src/api/client.ts"
      via: "import apiClient from './client'"
      pattern: "apiClient"
    - from: "frontend/src/hooks/useJDs.ts"
      to: "@tanstack/react-query"
      via: "import { keepPreviousData, useQuery } from '@tanstack/react-query'"
      pattern: "keepPreviousData"
---

<objective>
**Frontend Infrastructure: npm packages + shadcn components + TypeScript types + API functions + TanStack Query hooks**

安装 Phase 2 前端所需的所有依赖和 shadcn 组件，创建 JD 领域的数据类型定义、API 调用函数和 TanStack Query 封装 hooks。为 Plan 03 和 Plan 04 的 UI 开发提供基础。

Purpose: 建立前端 JD 管理所需的所有非 UI 基础设施，确保后续 UI 组件可以直接使用类型化的 API 和 hooks。
Output: 类型定义、API 函数、hooks、shadcn 组件、npm 依赖更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jd-management/02-CONTEXT.md
@.planning/phases/02-jd-management/02-RESEARCH.md

<interfaces>
Existing patterns to follow:

From frontend/src/api/client.ts:
```typescript
import axios from "axios";
const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || "",
  withCredentials: true,
  headers: { "Content-Type": "application/json" },
});
```
Pattern: Import apiClient for all API calls. Do NOT create a new axios instance.

From frontend/src/hooks/useAuth.ts:
```typescript
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import apiClient from "../api/client";
```
Pattern: TanStack Query hooks with useQuery / useMutation, query keys with ["domain", "action"] structure.

From frontend/package.json — existing dependencies:
- @tanstack/react-query: ^5.100.5 (v5 — crucial for placeholderData: keepPreviousData API)
- zod: ^4.3.6
- @hookform/resolvers: ^5.2.2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages and shadcn/ui components</name>
  <files>
    frontend/package.json
    frontend/package-lock.json
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/form.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/textarea.tsx
    frontend/src/components/ui/popover.tsx
    frontend/src/components/ui/calendar.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/command.tsx
    frontend/src/components/ui/dropdown-menu.tsx
  </files>
  <read_first>
    frontend/package.json — Current dependencies to confirm no conflicts. @tanstack/react-query @5.x, zod @4.x, @hookform/resolvers @5.x already installed.
  </read_first>
  <action>
    Step 1: Install npm packages for Phase 2.
    Run from /root/project/hellojobs/frontend:
    ```bash
    cd /root/project/hellojobs/frontend
    npm install @tanstack/react-table@8 react-hook-form@7 react-day-picker@9 date-fns@4
    ```

    Step 2: Install shadcn/ui components.
    Run from /root/project/hellojobs/frontend:
    ```bash
    cd /root/project/hellojobs/frontend
    npx shadcn@latest add table -y
    npx shadcn@latest add form -y
    npx shadcn@latest add select -y
    npx shadcn@latest add badge -y
    npx shadcn@latest add textarea -y
    npx shadcn@latest add popover -y
    npx shadcn@latest add calendar -y
    npx shadcn@latest add dialog -y
    npx shadcn@latest add command -y
    npx shadcn@latest add dropdown-menu -y
    ```

    Step 3: Verify installations.
    ```bash
    cd /root/project/hellojobs/frontend
    node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; ['@tanstack/react-table', 'react-hook-form', 'react-day-picker', 'date-fns'].forEach(d => console.log(d, deps[d] || 'MISSING'))"
    ```
    All four packages should show version numbers.

    Verify shadcn components exist:
    ```bash
    ls frontend/src/components/ui/table.tsx frontend/src/components/ui/form.tsx frontend/src/components/ui/select.tsx frontend/src/components/ui/badge.tsx frontend/src/components/ui/textarea.tsx frontend/src/components/ui/popover.tsx frontend/src/components/ui/calendar.tsx frontend/src/components/ui/dialog.tsx frontend/src/components/ui/command.tsx frontend/src/components/ui/dropdown-menu.tsx
    ```
    All ten files must exist.
  </action>
  <verify>
    <automated>ls /root/project/hellojobs/frontend/src/components/ui/table.tsx /root/project/hellojobs/frontend/src/components/ui/form.tsx /root/project/hellojobs/frontend/src/components/ui/select.tsx /root/project/hellojobs/frontend/src/components/ui/dialog.tsx 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    1. Grep confirms @tanstack/react-table in package.json: `grep -c "@tanstack/react-table" frontend/package.json` == 1
    2. Grep confirms react-hook-form in package.json: `grep -c "react-hook-form" frontend/package.json` == 1
    3. Grep confirms react-day-picker in package.json: `grep -c "react-day-picker" frontend/package.json` == 1
    4. Grep confirms date-fns in package.json: `grep -c "date-fns" frontend/package.json` == 1
    5. All 10 shadcn component files exist: ls for table.tsx, form.tsx, select.tsx, badge.tsx, textarea.tsx, popover.tsx, calendar.tsx, dialog.tsx, command.tsx, dropdown-menu.tsx all succeed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript types and API functions for JD</name>
  <files>
    frontend/src/types/jd.ts
    frontend/src/api/jd.ts
  </files>
  <read_first>
    frontend/src/types/jd.ts (does not exist yet — create new)
    frontend/src/api/client.ts — Pattern: `import apiClient from "./client"` for axios instance
    .planning/phases/02-jd-management/02-RESEARCH.md — Lines 960-1007 show exact TypeScript types and API functions
  </read_first>
  <action>
    Step 1: Create `frontend/src/types/jd.ts` with these exports:

    ```typescript
    export type JDStatus = "draft" | "active" | "closed";
    export type EducationLevel = "高中" | "大专" | "本科" | "硕士" | "博士" | "不限";

    export interface JD {
      id: number;
      title: string;
      department: string;
      skills: string;
      experience_years: number;
      education: EducationLevel;
      salary_min: number;
      salary_max: number;
      description: string;
      status: JDStatus;
      created_at: string;
      updated_at: string;
    }

    export interface JDListResponse {
      items: JD[];
      total: number;
      page: number;
      page_size: number;
    }

    export interface JDFormValues {
      title: string;
      department: string;
      skills: string;
      experience_years: number;
      education: EducationLevel;
      salary_min: number;
      salary_max: number;
      description: string;
    }

    export interface JDTemplate {
      name: string;
      description: string;
      skills: string;
      experience_years: number;
      education: EducationLevel;
      salary_min: number;
      salary_max: number;
      description: string;
    }
    ```

    Step 2: Create `frontend/src/api/jd.ts` with these exports:

    ```typescript
    import apiClient from "./client";
    import type { JD, JDListResponse, JDFormValues, JDTemplate } from "../types/jd";

    export async function fetchJDs(params: Record<string, string | number>): Promise<JDListResponse> {
      const searchParams = new URLSearchParams();
      Object.entries(params).forEach(([key, value]) => {
        if (value !== undefined && value !== "" && value !== null) {
          searchParams.set(key, String(value));
        }
      });
      const res = await apiClient.get<JDListResponse>(`/jd?${searchParams.toString()}`);
      return res.data;
    }

    export async function fetchJDTemplates(): Promise<JDTemplate[]> {
      const res = await apiClient.get<JDTemplate[]>("/jd/templates");
      return res.data;
    }

    export async function createJD(data: JDFormValues): Promise<JD> {
      const res = await apiClient.post<JD>("/jd", data);
      return res.data;
    }

    export async function updateJD(id: number, data: JDFormValues): Promise<JD> {
      const res = await apiClient.put<JD>(`/jd/${id}`, data);
      return res.data;
    }

    export async function updateJDStatus(id: number, status: string): Promise<JD> {
      const res = await apiClient.patch<JD>(`/jd/${id}/status`, { status });
      return res.data;
    }
    ```

    Verify both files compile:
    ```bash
    cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    1. File frontend/src/types/jd.ts exists with exports: JD, JDListResponse, JDFormValues, JDTemplate, JDStatus, EducationLevel
    2. File frontend/src/api/jd.ts exists with exports: fetchJDs, fetchJDTemplates, createJD, updateJD, updateJDStatus
    3. Grep confirms: `grep -c "export.*interface JD" frontend/src/types/jd.ts` == 1
    4. Grep confirms: `grep -c "export async function" frontend/src/api/jd.ts` == 5
    5. `npx tsc --noEmit` succeeds (no type errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create TanStack Query hooks for JD</name>
  <files>frontend/src/hooks/useJDs.ts</files>
  <read_first>
    frontend/src/hooks/useAuth.ts — Pattern: import from @tanstack/react-query, useQuery/useMutation, queryClient.invalidateQueries
    frontend/src/types/jd.ts (created in Task 2) — TypeScript interfaces
    frontend/src/api/jd.ts (created in Task 2) — API functions
    .planning/phases/02-jd-management/02-RESEARCH.md — Lines 396-435 show useJDs hook pattern with keepPreviousData (CRITICAL: v5 API)
  </read_first>
  <action>
    Create `frontend/src/hooks/useJDs.ts` with these hooks:

    ```typescript
    import { keepPreviousData, useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
    import { fetchJDs, fetchJDTemplates, createJD, updateJD, updateJDStatus } from "../api/jd";
    import type { JDListResponse, JDFormValues, JDTemplate, JD } from "../types/jd";

    export interface JDFilters {
      page: number;
      pageSize: number;
      status?: string;
      keyword?: string;
      dateFrom?: string;
      dateTo?: string;
    }

    export function useJDs(filters: JDFilters) {
      return useQuery<JDListResponse>({
        queryKey: ["jd", "list", filters],
        queryFn: () => fetchJDs({
          page: filters.page,
          page_size: filters.pageSize,
          ...(filters.status && { status: filters.status }),
          ...(filters.keyword && { keyword: filters.keyword }),
          ...(filters.dateFrom && { date_from: filters.dateFrom }),
          ...(filters.dateTo && { date_to: filters.dateTo }),
        }),
        placeholderData: keepPreviousData, // CRITICAL: v5 API — function, not boolean!
        staleTime: 30_000,
      });
    }

    export function useJDTemplates() {
      return useQuery<JDTemplate[]>({
        queryKey: ["jd", "templates"],
        queryFn: fetchJDTemplates,
        staleTime: Infinity, // Templates never change (D-11: HR cannot edit)
      });
    }

    export function useCreateJD() {
      const queryClient = useQueryClient();
      return useMutation<JD, Error, JDFormValues>({
        mutationFn: createJD,
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ["jd", "list"] });
        },
      });
    }

    export function useUpdateJD() {
      const queryClient = useQueryClient();
      return useMutation<JD, Error, { id: number; data: JDFormValues }>({
        mutationFn: ({ id, data }) => updateJD(id, data),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ["jd", "list"] });
        },
      });
    }

    export function useUpdateJDStatus() {
      const queryClient = useQueryClient();
      return useMutation<JD, Error, { id: number; status: string }>({
        mutationFn: ({ id, status }) => updateJDStatus(id, status),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ["jd", "list"] });
        },
      });
    }
    ```

    Verify the file compiles:
    ```bash
    cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -10
    ```
  </action>
  <verify>
    <automated>cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    1. File frontend/src/hooks/useJDs.ts exists
    2. Grep confirms: `grep -c "keepPreviousData" frontend/src/hooks/useJDs.ts` == 1 (v5 function import, not boolean)
    3. Grep confirms: `grep -c "export function use" frontend/src/hooks/useJDs.ts` == 5 (useJDs, useJDTemplates, useCreateJD, useUpdateJD, useUpdateJDStatus)
    4. `grep -c "placeholderData: keepPreviousData" frontend/src/hooks/useJDs.ts` == 1 (CORRECT v5 API)
    5. `npx tsc --noEmit` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | All API calls go through axios withCredentials. No user input in types/API/hooks layer — pure data typing and transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INFRA-01 | Information Disclosure | Type definitions | accept | TypeScript interfaces define data shapes only; no runtime impact. Types are stripped during compilation. |
| T-INFRA-02 | Tampering | API functions | mitigate | API functions use typed parameters (JDFormValues). Server-side Pydantic validation provides defense-in-depth. |
</threat_model>

<verification>
1. All npm packages installed: grep confirms @tanstack/react-table, react-hook-form, react-day-picker, date-fns in package.json
2. All 10 shadcn components exist in frontend/src/components/ui/
3. TypeScript compilation passes: `npx tsc --noEmit` returns exit code 0
4. Type definitions export correctly: `grep -c "export" frontend/src/types/jd.ts` >= 6
5. 5 API functions and 5 hooks defined
6. keepPreviousData imported as function (NOT boolean) — verified by grep
</verification>

<success_criteria>
1. `npx tsc --noEmit` from frontend/ exits with code 0
2. All 10 shadcn component files exist in frontend/src/components/ui/
3. All 4 new npm packages listed in frontend/package.json dependencies
4. TypeScript types, API functions, and hooks compile without errors
5. All locked decisions (D-05 through D-11) have frontend infrastructure ready (table, form, select, badge, calendar components installed for use by Plan 03/04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-jd-management/02-02-SUMMARY.md`
</output>
