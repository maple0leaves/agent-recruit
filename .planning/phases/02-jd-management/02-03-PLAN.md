---
phase: 02-jd-management
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/components/JDs/JDStatusBadge.tsx
  - frontend/src/components/JDs/JDTable.tsx
  - frontend/src/components/JDs/JDFilterBar.tsx
  - frontend/src/components/JDs/JDPagination.tsx
  - frontend/src/pages/JDManagement.tsx
autonomous: true
requirements:
  - JD-02
  - JD-03

must_haves:
  truths:
    - "HR can view a paginated table of JDs with columns: title, department, status, salary range, updated_at (D-05)"
    - "HR can filter JDs by status (draft/active/closed) via dropdown select"
    - "HR can search JDs by keyword (searches title, department, skills) via text input"
    - "HR can filter JDs by date range via date picker"
    - "HR can navigate pages with page number buttons and prev/next buttons (D-06: 20 per page)"
    - "Status is displayed as a colored badge (draft=gray, active=green, closed=red)"
    - "Table shows loading skeleton state and empty state"
    - "Each row has an action dropdown with Edit and Close/Reactivate options"
  artifacts:
    - path: "frontend/src/components/JDs/JDStatusBadge.tsx"
      provides: "Colored badge component for JD status display"
      min_lines: 20
    - path: "frontend/src/components/JDs/JDTable.tsx"
      provides: "TanStack Table wrapper rendering JD array"
      min_lines: 60
    - path: "frontend/src/components/JDs/JDFilterBar.tsx"
      provides: "Search input, status filter, date range picker"
      min_lines: 80
    - path: "frontend/src/components/JDs/JDPagination.tsx"
      provides: "Page number buttons with prev/next"
      min_lines: 50
    - path: "frontend/src/pages/JDManagement.tsx"
      provides: "Full JD list page combining all components"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/JDs/JDTable.tsx"
      to: "@tanstack/react-table"
      via: "import { useReactTable, getCoreRowModel, ... }"
      pattern: "useReactTable"
    - from: "frontend/src/pages/JDManagement.tsx"
      to: "frontend/src/hooks/useJDs.ts"
      via: "import { useJDs } from ..."
      pattern: "useJDs"
    - from: "frontend/src/pages/JDManagement.tsx"
      to: "frontend/src/components/JDs/"
      via: "Imports all sub-components"
      pattern: "import.*from.*JDs/"
---

<objective>
**Frontend: JD List Page with Table, Filter, Pagination**

构建 JD 列表页面：带筛选条（状态/关键词/日期范围）、shadcn Table + TanStack Table 渲染、传统分页组件、状态徽章。替换现有占位页。

Purpose: 实现 JD 列表展示（JD-03）和状态操作入口（JD-02 的 close/reactivate 下拉菜单）。
Output: 五个组件文件 + 完整的 JD 管理列表页。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jd-management/02-CONTEXT.md
@.planning/phases/02-jd-management/02-RESEARCH.md

<interfaces>
Dependencies provided by Plan 02:

From frontend/src/types/jd.ts:
```typescript
export type JDStatus = "draft" | "active" | "closed";
export interface JD { id: number; title: string; department: string; skills: string; experience_years: number; education: EducationLevel; salary_min: number; salary_max: number; description: string; status: JDStatus; created_at: string; updated_at: string; }
export interface JDListResponse { items: JD[]; total: number; page: number; page_size: number; }
```

From frontend/src/hooks/useJDs.ts:
```typescript
export interface JDFilters { page: number; pageSize: number; status?: string; keyword?: string; dateFrom?: string; dateTo?: string; }
export function useJDs(filters: JDFilters) -> useQuery result with placeholderData: keepPreviousData
export function useUpdateJDStatus() -> useMutation with invalidateQueries
```

Existing components available: @/components/ui/button, -/card, -/input, -/select, -/badge, -/table, -/popover, -/calendar, -/dropdown-menu, -/skeleton, -/label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JDStatusBadge, JDTable, JDFilterBar, JDPagination components</name>
  <files>
    frontend/src/components/JDs/JDStatusBadge.tsx
    frontend/src/components/JDs/JDTable.tsx
    frontend/src/components/JDs/JDFilterBar.tsx
    frontend/src/components/JDs/JDPagination.tsx
  </files>
  <read_first>
    frontend/src/types/jd.ts — JD, JDStatus types
    frontend/src/hooks/useJDs.ts — JDFilters, useJDs, useUpdateJDStatus
    frontend/src/components/ui/badge.tsx — Badge component API (variant prop)
    frontend/src/components/ui/table.tsx — Table markup structure (Table, TableHeader, TableBody, TableRow, TableHead, TableCell)
    frontend/src/components/ui/dropdown-menu.tsx — DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem
    frontend/src/components/ui/select.tsx — Select, SelectTrigger, SelectValue, SelectContent, SelectItem
    frontend/src/components/ui/popover.tsx — Popover, PopoverTrigger, PopoverContent
    frontend/src/components/ui/calendar.tsx — Calendar component (mode="range" or single, onSelect, selected)
    frontend/src/components/ui/skeleton.tsx — Skeleton component for loading state
    .planning/phases/02-jd-management/02-RESEARCH.md — Patterns 4 (537-612) and 5 (1012-1063) show JDTable and JDPagination examples
  </read_first>
  <action>
    Create the directory first:
    ```bash
    mkdir -p /root/project/hellojobs/frontend/src/components/JDs
    ```

    **JDStatusBadge.tsx:**
    Create a component that accepts `status: JDStatus` and renders a shadcn Badge with specific colors:
    - draft: variant="secondary" (gray) with label "草稿"
    - active: variant="default" with green background (override via className "bg-green-100 text-green-800 hover:bg-green-100") with label "发布中"
    - closed: variant="outline" with red text ("text-red-500 border-red-200") with label "已关闭"
    Use `import { cva } from "class-variance-authority"` for variant styling or inline Tailwind classes.

    **JDPagination.tsx:**
    Create a component accepting `page, pageSize, total, onPageChange` props (D-06: traditional pagination, 20/page).
    - Calculate `totalPages = Math.ceil(total / pageSize)`
    - Show "共 {total} 条，第 {page}/{totalPages} 页" text on the left
    - Generate page number buttons:
      - Always show first page
      - Show pages around current page (current-2 to current+2, clamped to [1, totalPages])
      - Always show last page
      - Use "..." ellipsis when there are gaps
    - Previous/Next buttons with disabled state at boundaries
    - Use shadcn Button with variant={p === page ? "default" : "outline"} size="sm"
    - Import: `import { Button } from "@/components/ui/button"` and `import { ChevronLeft, ChevronRight } from "lucide-react"`

    **JDTable.tsx:**
    Create component accepting `data: JD[], onEdit: (jd: JD) => void` props.
    - Use `@tanstack/react-table`: import `useReactTable`, `getCoreRowModel`, `getSortedRowModel`, `flexRender`, `ColumnDef`, `SortingState`
    - Define columns array (D-05 table layout):
      1. title: accessorKey="title", header="职位名称", enableSorting: true
      2. department: accessorKey="department", header="部门", enableSorting: true
      3. status: display cell using JDStatusBadge component (cell uses row.original.status)
      4. salary range: custom cell showing "{salary_min}-{salary_max}" or "面议" if both 0
      5. updated_at: accessorKey="updated_at", header="更新日期", cell formatting date with `toLocaleDateString("zh-CN")`
      6. actions: custom cell with DropdownMenu:
         - DropdownMenuTrigger=Button with "more-horizontal" icon (MoreHorizontal from lucide-react)
         - DropdownMenuItem "编辑" onClick fires onEdit(row.original)
         - DropdownMenuItem "发布" / "关闭" / "重新激活" (depending on current status):
           - draft: "发布" (发布中)
           - active: "关闭"
           - closed: "重新激活"
         - Import: `import { MoreHorizontal } from "lucide-react"` and `import { Button } from "@/components/ui/button"`
    - Use `useState<SortingState>([])` for client-side sorting
    - Table markup: `<Table>` -> `<TableHeader>` -> header groups -> `<TableBody>` -> row model
    - Render empty state when no data: "暂无JD数据" in a centered table cell with colspan=6

    **JDFilterBar.tsx:**
    Create component accepting `filters: JDFilters, onFilterChange: (filters: Partial<JDFilters>) => void` props.
    Layout: horizontal flex row with gap-4, items-center.
    - Keyword search Input:
      - `<Input placeholder="搜索职位名称/部门/技能..." className="w-64"` 
      - Controlled by `filters.keyword`
      - Debounce 300ms before calling onFilterChange (use setTimeout/clearTimeout in useEffect)
    - Status filter Select (D-08 filter dimensions):
      - `<Select value={filters.status || "all"} onValueChange={...}>`
      - Options: "全部" (value="all"), "草稿" (value="draft"), "发布中" (value="active"), "已关闭" (value="closed")
      - When "all" selected, pass undefined to onFilterChange (removes status filter)
    - Date range filter (D-08 filter dimensions):
      - Use shadcn Popover + Calendar for date range
      - Button shows selected date range or "日期范围" placeholder
      - Calendar with mode="range" (react-day-picker)
      - Import date formatting helpers from date-fns (format function)
      - When dates selected, call onFilterChange with dateFrom/dateTo as ISO date strings "YYYY-MM-DD"
      - Include a clear button to reset date range
    - Reset button: "重置" that clears all filters (calls onFilterChange with empty values)
    - Labels for each filter group as needed

    Verify all 4 files compile:
    ```bash
    cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    1. 4 component files exist in frontend/src/components/JDs/
    2. JDStatusBadge.tsx imports Badge from ui/badge and accepts `status: JDStatus` prop
    3. JDTable.tsx imports `useReactTable` from @tanstack/react-table
    4. JDTable.tsx uses MoreHorizontal from lucide-react for actions column
    5. JDFilterBar.tsx imports Select, Input, Popover, Calendar components
    6. JDPagination.tsx imports Button from ui/button and renders page numbers
    7. `npx tsc --noEmit` succeeds
    8. Grep confirms: `grep -c "useReactTable" frontend/src/components/JDs/JDTable.tsx` == 1
    9. Grep confirms: `grep -c "JDStatusBadge" frontend/src/components/JDs/JDTable.tsx` == 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace JDManagement.tsx with full list page</name>
  <files>frontend/src/pages/JDManagement.tsx</files>
  <read_first>
    frontend/src/pages/JDManagement.tsx — Current placeholder page (5 lines of JSX with Card). Will be fully replaced.
    frontend/src/hooks/useJDs.ts — useJDs hook signature, JDFilters interface
    frontend/src/components/JDs/JDStatusBadge.tsx (created in Task 1)
    frontend/src/components/JDs/JDTable.tsx (created in Task 1)
    frontend/src/components/JDs/JDFilterBar.tsx (created in Task 1)
    frontend/src/components/JDs/JDPagination.tsx (created in Task 1)
  </read_first>
  <action>
    Completely replace `frontend/src/pages/JDManagement.tsx`. The new component:

    **State management:**
    - `const [filters, setFilters] = useState<JDFilters>({ page: 1, pageSize: 20 })`
    - `const { data, isLoading, isPlaceholderData } = useJDs(filters)`
    - `const updateStatusMutation = useUpdateJDStatus()`
    - `const [editingJD, setEditingJD] = useState<JD | null>(null)` (for Plan 04 integration — set to null for now)
    - `const [dialogOpen, setDialogOpen] = useState(false)` (for Plan 04 integration — set to false for now)

    **Layout:**
    - Top section: Page header with title "JD管理" and a "新建 JD" button (Button onClick={} — empty handler for Plan 04 to wire up)
    - Filter bar section: `<JDFilterBar filters={filters} onFilterChange={(updates) => setFilters(prev => ({ ...prev, ...updates, page: 1 }))} />`
      - When filters change, always reset page to 1
    - Table section:
      - Loading state (isLoading && !isPlaceholderData): Show Skeleton rows (4 rows of Skeleton with h-12)
      - Data state: `<JDTable data={data?.items || []} onEdit={(jd) => setEditingJD(jd)} />`
      - Empty state (data?.items.length === 0 && !isLoading): Show empty state message "暂无JD数据，点击上方按钮创建" with an icon
    - Pagination section: `<JDPagination page={filters.page} pageSize={filters.pageSize} total={data?.total || 0} onPageChange={(p) => setFilters(prev => ({ ...prev, page: p }))} />`

    **Handle status change from dropdown:**
    - Pass `onStatusChange` callback to JDTable (or handle via the edit click event)
    - The action column DropdownMenu items call `updateStatusMutation.mutate({ id: jd.id, status: newStatus })`
    - Show toast or alert on error

    **Integrate Edit dialog for Plan 04:**
    - Include a Dialog component with `open={dialogOpen}` but the dialog content is an empty placeholder `<DialogContent><p>编辑功能</p></DialogContent>`
    - Plan 04 will replace this with the actual JDForm
    - The "编辑" DropdownMenuItem sets `editingJD` and opens the dialog

    **Imports:**
    ```typescript
    import { useState } from "react";
    import { useJDs, useUpdateJDStatus, type JDFilters } from "../hooks/useJDs";
    import JDTable from "../components/JDs/JDTable";
    import JDFilterBar from "../components/JDs/JDFilterBar";
    import JDPagination from "../components/JDs/JDPagination";
    import { Button } from "../components/ui/button";
    import { Skeleton } from "../components/ui/skeleton";
    import { FileText } from "lucide-react";
    // Dialog imports for Plan 04 integration stub:
    import { Dialog, DialogContent, DialogHeader, DialogTitle } from "../components/ui/dialog";
    import type { JD } from "../types/jd";
    ```

    Verify compilation:
    ```bash
    cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd /root/project/hellojobs/frontend && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    1. File frontend/src/pages/JDManagement.tsx has been rewritten (not the placeholder anymore)
    2. Grep confirms: `grep -c "useJDs" frontend/src/pages/JDManagement.tsx` == 1
    3. Grep confirms: `grep -c "JDFilterBar" frontend/src/pages/JDManagement.tsx` == 1
    4. Grep confirms: `grep -c "JDTable" frontend/src/pages/JDManagement.tsx` == 1
    5. Grep confirms: `grep -c "JDPagination" frontend/src/pages/JDManagement.tsx` == 1
    6. Grep confirms: `grep -c "useUpdateJDStatus" frontend/src/pages/JDManagement.tsx` == 1
    7. Grep confirms: `grep -c "Dialog" frontend/src/pages/JDManagement.tsx` >= 3 (Dialog, DialogContent imports)
    8. Old placeholder strings "功能开发中" and "敬请期待" are NOT present in the file
    9. `npx tsc --noEmit` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Filter parameters (keyword, status, dates) are sent as URL query params. No sensitive data in filter inputs. |
| Dropdown menu actions | Close/reactivate actions trigger API calls via useUpdateJDStatus mutation. All operations require auth (backend-enforced). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LIST-01 | Information Disclosure | Keyword search | accept | Search is server-side, no client-side data exposure. Keywords are plain text, not sensitive. |
| T-LIST-02 | Tampering | Status dropdown actions | mitigate | Backend enforces D-12 state machine. Frontend suggests valid actions but backend validates. Error from API displayed to user. |
| T-LIST-03 | Denial of Service | Date range filter | accept | Date range is validated server-side. No performance impact on frontend. |
</threat_model>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` exits with code 0
2. All 4 components + page compile without type errors
3. Page renders all sections: filter bar, table, pagination
4. Status badge renders 3 different styles (draft/active/closed)
5. Pagination shows correct page numbers and boundaries
6. Table columns match D-05 specification
</verification>

<success_criteria>
1. `npx tsc --noEmit` from frontend/ exits with code 0
2. JDStatusBadge renders shadcn Badge for all 3 status values
3. JDTable uses @tanstack/react-table correctly with sorting
4. JDFilterBar has keyword (debounced), status select, date range (Calendar popover), and reset button
5. JDPagination calculates totalPages correctly and handles edge cases (1 page, many pages, ellipsis)
6. JDManagement.tsx integrates all components with loading/empty/data states
7. Plan 04 can build on this page by replacing Dialog placeholder with actual form
</success_criteria>

<output>
After completion, create `.planning/phases/02-jd-management/02-03-SUMMARY.md`
</output>
